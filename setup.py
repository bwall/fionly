#!/usr/bin/env python

from distutils.core import setup

setup(name='fionly',
      version='1.1.0',
      description='Prints out the first unique instance of a line',
      author='Brian Wallace',
      author_email='bwall@ballastsecurity.net',
      url='https://github.com/bwall/fionly',
      packages=[],
      scripts=['fionly'],
     )